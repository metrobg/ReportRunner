# ReportRunner
Jasper Report Runner Servlet — Maven/Java 13/Tomcat 9 migration of the legacy ReportServlet.

Copy `src/main/resources/Connections.xml.sample` to `Connections.xml` and fill in real DB credentials before running; `Connections.xml` is gitignored.
