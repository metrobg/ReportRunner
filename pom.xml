<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>
    <repositories>
        <repository>
            <id>jaspersoft-third-party</id>
            <url>https://jaspersoft.jfrog.io/jaspersoft/third-party-ce-artifacts/</url>
        </repository>
    </repositories>

    <groupId>com.metrobg</groupId>
    <artifactId>ReportRunner</artifactId>
    <version>1.0-SNAPSHOT</version>
    <packaging>war</packaging>
    <dependencies>
        <dependency>
            <groupId>javax.servlet</groupId>
            <artifactId>javax.servlet-api</artifactId>
            <version>4.0.1</version>
            <scope>provided</scope>
        </dependency>
        <dependency>
            <groupId>net.sf.jasperreports</groupId>
            <artifactId>jasperreports</artifactId>
            <version>6.21.0</version>
            <exclusions>
                <exclusion>
                    <groupId>com.lowagie</groupId>
                    <artifactId>itext</artifactId>
                </exclusion>
            </exclusions>
        </dependency>
        <dependency>
            <groupId>net.sf.jasperreports</groupId>
            <artifactId>jasperreports-fonts</artifactId>
            <version>6.21.0</version>
        </dependency>
        <dependency>
            <groupId>com.github.librepdf</groupId>
            <artifactId>openpdf</artifactId>
            <version>1.3.30</version>
        </dependency>
        <dependency>
            <groupId>com.oracle.database.jdbc</groupId>
            <artifactId>ojdbc8</artifactId>
            <version>21.23.0.0</version>
        </dependency>
        <dependency>
            <groupId>org.codehaus.groovy</groupId>
            <artifactId>groovy-all</artifactId>
            <version>2.4.5</version>
        </dependency>

    </dependencies>
    <properties>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
    </properties>

    <build>
        <finalName>ReportRunner</finalName>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <version>3.13.0</version>
                <configuration>
                    <release>8</release>
                </configuration>
            </plugin>
            <plugin>
                <!--
                  jackson-annotations, woodstox-core, and stax2-api ship a
                  module-info.class (Java 9+ JPMS descriptor) that Tomcat's
                  bundled BCEL-based annotation scanner cannot parse, throwing
                  ClassFormatException and aborting context startup. None of
                  these jars contain @WebServlet/@WebFilter/@WebListener
                  annotations Tomcat needs to discover, so strip module-info.class
                  from them at build time rather than requiring a server-side
                  catalina.properties change. Sourced straight from the local
                  repo (not WEB-INF/lib) since that's populated before the war
                  plugin runs, avoiding any dependency on packaging phase order.
                -->
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-antrun-plugin</artifactId>
                <version>3.1.0</version>
                <executions>
                    <execution>
                        <id>strip-module-info</id>
                        <phase>generate-resources</phase>
                        <goals>
                            <goal>run</goal>
                        </goals>
                        <configuration>
                            <target>
                                <property name="stripped" value="${project.build.directory}/stripped-lib"/>
                                <property name="workdir" value="${project.build.directory}/module-info-strip"/>
                                <mkdir dir="${stripped}"/>

                                <macrodef name="strip-module-info-from-jar">
                                    <attribute name="srcjar"/>
                                    <attribute name="jarname"/>
                                    <sequential>
                                        <delete dir="${workdir}/@{jarname}"/>
                                        <unzip src="@{srcjar}" dest="${workdir}/@{jarname}">
                                            <patternset>
                                                <exclude name="module-info.class"/>
                                                <exclude name="META-INF/versions/*/module-info.class"/>
                                            </patternset>
                                        </unzip>
                                        <zip destfile="${stripped}/@{jarname}" basedir="${workdir}/@{jarname}"/>
                                    </sequential>
                                </macrodef>

                                <strip-module-info-from-jar
                                    srcjar="${settings.localRepository}/com/fasterxml/jackson/core/jackson-annotations/2.15.3/jackson-annotations-2.15.3.jar"
                                    jarname="jackson-annotations-2.15.3.jar"/>
                                <strip-module-info-from-jar
                                    srcjar="${settings.localRepository}/com/fasterxml/woodstox/woodstox-core/6.5.1/woodstox-core-6.5.1.jar"
                                    jarname="woodstox-core-6.5.1.jar"/>
                                <strip-module-info-from-jar
                                    srcjar="${settings.localRepository}/org/codehaus/woodstox/stax2-api/4.2.1/stax2-api-4.2.1.jar"
                                    jarname="stax2-api-4.2.1.jar"/>
                            </target>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-war-plugin</artifactId>
                <version>3.4.0</version>
                <configuration>
                    <failOnMissingWebXml>false</failOnMissingWebXml>
                    <webResources>
                        <resource>
                            <directory>${project.build.directory}/stripped-lib</directory>
                            <targetPath>WEB-INF/lib</targetPath>
                        </resource>
                    </webResources>
                </configuration>
            </plugin>
        </plugins>
    </build>

</project>